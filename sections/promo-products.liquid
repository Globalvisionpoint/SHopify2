<section class="promo-products section-space section-space--lg"
  {% if section.settings.background_image != blank %}style="background-image: url('{{ section.settings.background_image | image_url: width: 1920 }}'); background-position-y: {{ section.settings.background_position }}%;"{% endif %}>
  <div class="page-width">
    <div class="section-bar">
      <h2 class="section-bar__title">{{ section.settings.heading }}</h2>
      {% if section.settings.collection != blank %}
        <a href="{{ section.settings.collection.url }}" class="section-bar__link" aria-label="Vezi toate produsele">
          {% render 'icon', name: 'arrow-right' %}
        </a>
      {% endif %}
    </div>

    <div class="promo-products__backdrop" aria-hidden="true">%</div>

    {% assign featured_collection = section.settings.collection %}
    {% if featured_collection != blank and featured_collection.products_count > 0 %}
      <div class="rail-carousel" data-rail-carousel>
        <button class="rail-carousel__arrow rail-carousel__arrow--prev" type="button" aria-label="Produse anterioare" data-rail-prev>
          {% render 'icon', name: 'arrow-right' %}
        </button>
        <div class="product-rail" data-rail-track>
          {% for product in featured_collection.products limit: section.settings.products_to_show %}
            {% render 'product-card', product: product %}
          {% endfor %}
        </div>
        <button class="rail-carousel__arrow rail-carousel__arrow--next" type="button" aria-label="Produse urmatoare" data-rail-next>
          {% render 'icon', name: 'arrow-right' %}
        </button>
        <div class="rail-carousel__dots" data-rail-dots></div>
      </div>
    {% else %}
      <div class="rail-carousel" data-rail-carousel>
        <button class="rail-carousel__arrow rail-carousel__arrow--prev" type="button" aria-label="Produse anterioare" data-rail-prev>
          {% render 'icon', name: 'arrow-right' %}
        </button>
        <div class="product-rail" data-rail-track>
          {% for index in (1..4) %}
            <article class="product-card product-card--placeholder">
              <div class="product-card__media">{{ 'product-' | append: index | placeholder_svg_tag: 'product-card__placeholder-image' }}</div>
              <div class="product-card__body">
                <p class="product-card__title">Produs demonstrativ {{ index }}</p>
                <div class="price-block">
                  <span class="price-block__current">99,00 lei</span>
                </div>
              </div>
            </article>
          {% endfor %}
        </div>
        <button class="rail-carousel__arrow rail-carousel__arrow--next" type="button" aria-label="Produse urmatoare" data-rail-next>
          {% render 'icon', name: 'arrow-right' %}
        </button>
        <div class="rail-carousel__dots" data-rail-dots></div>
      </div>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Promo products",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Titlu",
      "default": "Noutati"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Colectie"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "label": "Numar produse",
      "min": 4,
      "max": 12,
      "step": 1,
      "default": 6
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Imagine fundal"
    },
    {
      "type": "range",
      "id": "background_position",
      "label": "Pozitie imagine (sus=0, jos=100)",
      "min": 0,
      "max": 100,
      "step": 5,
      "default": 50
    }
  ],
  "presets": [
    {
      "name": "Promo products"
    }
  ]
}
{% endschema %}